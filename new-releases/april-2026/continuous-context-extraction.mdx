Domain knowledge doesn't stay accurate on its own. Metric definitions, business rules, and table descriptions live across GitHub repositories, dbt YAML files, Looker models, and data catalogs. The moment any of those sources changes, your domain starts falling behind.

Continuous Context Extraction (ACE) closes that gap. You can now connect your domain directly to the systems where your business logic lives. ACE runs extraction on a scheduled or on-demand basis, detects what changed since the last run, and surfaces only new signals for your review. This means your domain stays perpetually aligned with the source of truth without requiring you to manually export or upload a file.

## How it works

Continuous Context Extraction follows four stages from connection to a published domain update.

<Steps>
  <Step title="Connect a source" icon="plug">
    On your domain's **Context Extractor** page, select the **Connections** tab and click **Add Connection**. A five-step wizard walks you through choosing a source type, authenticating (API key, OAuth, or Personal Access Token), scoping the extraction (a specific repository or org-wide), setting a schedule, and confirming. Credentials are validated immediately and the connection appears with **PENDING** status.
  </Step>
  <Step title="Run extraction" icon="arrows-rotate">
    Once connected, ACE runs extraction automatically on your configured schedule. The first run is a full scan of the connected source. Subsequent incremental runs process only changes since the last extraction, staying lightweight and focused. You can trigger a manual extraction at any time using **Extract Now**. The connection status updates in real time: **EXTRACTING** while the job runs, **AWAITING REVIEW** when it completes with items for your review, **ACTIVE** once all review items are resolved and the domain is updated, and **ERROR** if something goes wrong (previous context is retained and a manual re-run is available). When extraction completes and review is needed, you receive an in-app notification with the count of items requiring attention and a direct link to the review screen.
  </Step>
  <Step title="Review conflicts and duplicates" icon="list-check">
    When extraction completes, ACE compares every incoming context object against your existing domain. Conflicting entries surface as update recommendations, suggested changes for your review before anything is applied. After conflict resolution, an embedding-based similarity pass flags semantically equivalent entries as duplicate candidates. You resolve each item in the review queue: accept, decline, merge, keep both, or defer. No changes are published until you explicitly approve.
  </Step>
  <Step title="Publish and version your domain" icon="rocket">
    After clearing the review queue, click **Publish**. ACE increments your domain version number, stores a full snapshot, and updates chat and dashboards immediately. Every published version is retained in the **Version History** panel. If a published version introduces issues, select any prior version and restore it in a single confirmation step. Rollback is append-only and nothing is permanently deleted.
  </Step>
</Steps>

<Note>
  Nothing in your domain is ever updated automatically. Every extraction passes through a human-in-the-loop review. Conflicts must be resolved and the dedup queue cleared before a new version is published. There is no auto-merge in the current release.
</Note>

## Key capabilities

• **Source connections**: Connect to GitHub using a read-only Personal Access Token (PAT), scoped to a single repository or an entire organization. Looker, dbt, Atlan, Glean, and Collibra are rolling out in the same sprint cycle and will appear in the connection wizard as they go live. No write access is required.

• **Scheduled and on-demand extraction**: Extraction runs daily at 2:00 AM local time by default; adjust the time during connection wizard setup. **Extract Now** triggers an immediate incremental run at any time, independent of the schedule. The first run is a full scan of the connected source; subsequent runs process only changes since the last extraction. Jobs run as persistent background processes; you can close your browser mid-extraction and return later, as status is saved every 60 seconds.

• **Query history extraction** (gated): For enabled domains, ACE reads recently executed SQL from your connected warehouse and extracts metric definitions, JOIN patterns, business conventions, and reviewed query pairs. These signals feed the same review queue as source-based extractions. Contact your Solutions Engineer to confirm whether query history extraction is enabled for your environment.

• **Conflict resolution**: After each extraction, ACE compares incoming context objects against existing domain entries by name. Definition mismatches are flagged as conflicts and surfaced as update recommendations in the review queue. The domain is not updated until every conflict is resolved or deferred. All resolution decisions are logged with user ID and timestamp. A dedicated side-by-side diff view with options to accept either source, write a custom merged definition, or defer is on the near-term roadmap.

• **Semantic duplicate detection**: After conflict resolution, ACE runs an embedding-based similarity pass across all context objects in the domain. Entries that mean the same thing, even if worded differently across sources, are flagged as duplicate candidates. Each candidate pair shows a similarity score and a suggested canonical definition; you can accept a merge, keep both entries, or defer. The dedup queue must be cleared before publishing a new domain version. No merges happen automatically.

• **Version history and rollback**: Each published extraction increments the domain version number and stores a full snapshot. The **Version History** panel shows version number, publish timestamp, author, change count, and a one-line summary; diffs between any two versions are available on demand. To roll back, select a prior version and confirm in a single step; rollback creates a new version entry rather than overwriting history. History is append-only; nothing is permanently deleted. Chat and dashboards reflect the rolled-back domain immediately.

## Target users

Continuous Context Extraction is designed for:

• **Domain Admins** who manage the knowledge layer for their WisdomAI domain and need it to stay synchronized with evolving business logic across engineering and analytics sources.

• **Data and analytics teams** that maintain metric definitions, JOIN logic, and business rules in GitHub, dbt, or Looker and want domain updates to flow automatically from source changes rather than manual uploads.

Explorers and analysts are not required to take any action; they benefit through improved answer quality and richer **Context used** explanations on query responses.

## User Stories

See real stories of how WisdomAI improvements are helping solve real user problems:

### Story 1: Keeping Metrics Aligned After a dbt Model Refactor

A data engineering team refactors their `revenue` model in dbt, changing the grain and updating metric definitions across three YAML files. Previously, a domain lead would only discover the discrepancy after getting a wrong answer in chat. With Continuous Context Extraction, the next morning's incremental extraction picks up the dbt changes. The domain admin receives a notification: "3 conflicts detected. ARR definition differs between dbt and existing domain." They open the side-by-side diff, accept the updated dbt definition for ARR, write a custom merged definition for one edge case, and publish. The domain reflects the new business logic before analysts start their day.

### Story 2: Catching a Knowledge Regression with Rollback

After a scheduled extraction from Looker, a domain admin approves and publishes a new version. The next day, analysts report that revenue calculations are returning unexpected values. The admin opens the version history, diffs the new version against the previous one, and spots that an updated Looker dimension description inadvertently overrode a carefully tuned business rule.

They roll back to the prior version in one confirmation step. The domain reverts immediately. They then re-run the extraction, this time declining the problematic Looker entry and preserving the existing business rule. No data is lost; every version is retained in the history log, and the rollback itself is recorded as a new version.

## FAQs

<AccordionGroup>
  <Accordion title="What sources are supported today?">
    GitHub is live in the current release. Looker and dbt are available via the GitHub connector since they are typically stored in customer Git environments. Atlan, Glean, and Collibra are shipping in the same sprint cycle and will appear in the connection wizard as they go live.
  </Accordion>

  <Accordion title="Does anything get published to my domain automatically?">
    No. Every extraction goes through a human-in-the-loop review. Conflicts must be resolved and the dedup queue cleared before any version is published. There is no auto-merge in the current release.
  </Accordion>

  <Accordion title="What happens if I close my browser mid-extraction?">
    Nothing is interrupted. Extraction jobs run as persistent background processes. Status is persisted every 60 seconds. You can close your browser, come back later, and the job will still be running or waiting for your review when you return.
  </Accordion>

  <Accordion title="What is the difference between a conflict and a duplicate?">
    A conflict is when the same named term has different definitions across sources, detected by name match. A duplicate is when two differently-named entries are semantically equivalent, detected by embedding similarity. Both require your review before a new version is published.
  </Accordion>

  <Accordion title="What if I publish something and later realize it was wrong?">
    Use the rollback feature. Open the **Version History** panel, find the prior version you want, and restore it with a single confirmation step. Rollback creates a new version entry. History is append-only, so nothing is ever permanently deleted.
  </Accordion>

  <Accordion title="Can I have multiple sources connected at once?">
    Yes. You can have multiple active connections simultaneously. For example, GitHub and Looker at the same time. Each runs its own daily extraction job. All extracted suggestions enter the same conflict and dedup review queue, which spans across sources.
  </Accordion>

  <Accordion title="What permissions does ACE need for a GitHub connection?">
    A Personal Access Token with read access to the repositories you want to extract from. For org-wide extraction, a token with `repo` scope on the organization. No write permissions are required at any point.
  </Accordion>

  <Accordion title="How is this different from the file upload Context Extractor?">
    The original Context Extractor is a one-time, file-based flow: export a file, upload it, review suggestions, and publish. Continuous Context Extraction connects ACE directly to the live source system, extracts automatically on a schedule, and keeps the domain continuously aligned; no manual export or file handling required.
  </Accordion>

  <Accordion title="What happens if I disconnect a source?">
    The connection is removed and credentials are revoked immediately. Any in-progress extraction job is cancelled. Context that was already extracted and published to your domain remains by default; it is not removed automatically. You can manually delete any entries you no longer want to keep.
  </Accordion>

  <Accordion title="Is there a limit on how many connections I can have?">
    There is no hard cap in the current release. Any per-organization limits will be communicated before they apply as usage scales.
  </Accordion>

  <Accordion title="How does this relate to Query History Mining?">
    Query History Mining reads historical SQL from your warehouse to infer metric definitions, JOIN patterns, and business conventions from how analysts already query your data. It is a bottom-up signal derived from usage patterns. Continuous Context Extraction reads authoritative source-of-truth artifacts such as your dbt models, Looker explores, and governance catalog entries from the top down. Both are complementary and both feed the same review queue. Query history extraction is available today but gated to specific customers and domains; contact your Solutions Engineer to confirm whether it is enabled for your environment.
  </Accordion>
</AccordionGroup>

## Next steps

<CardGroup cols={2}>
  <Card title="Connect Your First Source" icon="plug" href="/features/continuous-context-extraction/connect-a-source">
    Step-by-step instructions for setting up your first GitHub connection.
  </Card>

  <Card title="Review Queue Guide" icon="list-check" href="/features/continuous-context-extraction/review-queue">
    Learn how to resolve conflicts, clear duplicate candidates, and publish a new domain version.
  </Card>
</CardGroup>